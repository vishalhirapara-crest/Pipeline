pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build the app'
            }
        }
        stage('Test') {
            steps {
                echo 'Test the app'
            }
        }
        stage('Deploye') {
            steps {
                echo 'Deploye the app'
            }
        }
    }
}
